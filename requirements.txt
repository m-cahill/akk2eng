# Runtime (install: pip install -r requirements.txt)
# Match pyproject.toml. Blackwell (sm_120) needs torch 2.10+cu128 (local override).
numpy>=1.26.0,<2
pandas>=2.0
torch>=2.4.0
transformers>=4.45.0,<4.48.0
datasets>=2.18.0,<4
accelerate>=0.26.0,<2
sentencepiece>=0.2.0
